# 问答记录
**时间**：2026-04-06 10:58:21 +0800
**提问**：如图 1 中，用户预约后，失约未到店，单号显示为 未起单(如图 3，检查并使用自定义skills ，把为起单替换为商家信息-起单/结单设置的当前使用名词)；如图 2，在用户预约并失约的使用记录中，增加显示状态：失约-用户未到店，并增加服务结束项显示，服务结束时间为，当前可预约可服务的截止时间；
**回答**：已在用户端卡片详情页完成修改。失约预约回落到使用记录后，单号位不再显示预约号，而是显示按商家“起单/结单设置”解析后的当前起单名词对应的“未起单”文案；同时保留预约号单独展示用于追踪。使用记录详情新增“状态：失约-用户未到店”，并补充“服务结束”字段，时间取当前预约可到店可服务的截止时间。已执行 `npm run build:user`，构建通过。
