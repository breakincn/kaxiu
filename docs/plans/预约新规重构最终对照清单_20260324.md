# 预约新规重构最终对照清单

日期：2026-03-24

对照文档：

- `docs/plans/预约新规与预约资源锁定补偿一体化方案_20260319.md`
- `docs/plans/预约新规与预约资源锁定补偿一体化方案_研发启动执行清单_20260322.md`

## 1. 复核结论

当前代码已经完成预约新规重构的主干开发，`P0 / P1 / P2` 的后端主链、预约结算闭环、拖堂账本兑现、不可抗力撤销、用户端与商户端核心页面均已落地，且当前回归状态为：

- `cd backend && go test ./... -count=1` 通过
- `cd frontend && npm run build:user` 通过
- `cd frontend && npm run build:merchant` 通过

当前已补齐此前尾项，按两份文档逐项复核后，可以判定：

- `预约新规核心开发已完成`
- `文档验收项已完成收口`

## 2. 已完成项

### 2.1 P0 主链

- 预约创建时已写入：
  - `reserved_start_at`
  - `reserved_end_at`
  - `occupied_end_at`
  - `cancel_deadline_at`
  - `booking_close_deadline_at`
  - `booking_root_id`
- 三阶段改签资格已落地：
  - `today_or_tomorrow`
  - `tomorrow_only`
  - `forbidden`
- 用户主动改签与商户保护性改签均已落地。
- 商户取消与取消申请已按 `5` 小时阈值分流。
- 用户端已支持：
  - 发起取消申请
  - 同意/拒绝商户取消申请
  - 提交抗辩意见
- 保护性改签场景已接入“高质量修复方案不存在时转取消/取消申请”的后端约束。
- 改签时段接口已按文档要求实现阶段化过滤：
  - 阶段 A：返回“更优于当前”与“不劣于当前”
  - 阶段 B/C：仅返回“更优于当前”

### 2.2 P1 履约与归责

- 签到建单已打通。
- 预约来源服务会话完成/失败/未起单完成均已统一回写预约结算快照。
- `appointment_waiting` 的降级、恢复、人工改派责任保持规则已补齐并有测试覆盖。
- 改签迁移结算已落地。
- 商户违约待判定、违约收敛、`merchant_failure_offset` 已落地。
- 商户直接取消、取消申请同意/拒绝、用户抗辩均已接入结算语义。
- 不可抗力申请、同意、拒绝、补偿撤销已落地。
- 月度免责次数、责任账本、月度异常统计接口已落地。

### 2.3 P2 拖堂补偿

- `appointment_delay_ledgers` 已落地。
- 项目级拖堂补偿参数已落地并支持前端配置。
- 服务开始回写 `actual_start_at` 已落地。
- 服务完成自动生成拖堂账本已落地。
- 拖堂账本兑现到 `appointment_compensations` 已落地。
- `amount_bucket / fixed_unit / extra_times / extend_minutes` 等兑现路径已落地。
- 部分兑现后的余量保留逻辑已修正并有测试覆盖。
- 用户端和商户端均已展示结算、拖堂账本与补偿结果。

### 2.4 已完成的前端展示

- 用户端预约详情已展示：
  - 锁定开始/结束时间
  - 直接取消截止时间
  - 商户取消原因
  - 用户抗辩意见
  - 结算状态
  - 责任归属
  - 拖堂账本
  - 补偿结果
  - 不可抗力申请状态
- 商户端预约详情已展示：
  - 结算状态
  - 责任归属
  - 锁定开始/结束时间
  - 直接取消截止时间
  - 商户取消原因
  - 用户抗辩意见
  - 客服月度异常统计入口
  - `balanced / D-1 16:00 / D-1 18:00` 规则说明
- 用户端与商户端改签弹窗均已展示阶段提示与时段标签：
  - `更优于当前`
  - `不劣于当前`
- 用户端与商户端取消动作均已接入“先推荐改签，再确认取消”分流。
- 用户端已补系统优化性改签推荐卡片：
  - 默认关闭
  - 开启后仅展示推荐时段与推荐原因
  - 不直接替用户提交改签
- 商户端已补排班发布与异常修复入口：
  - 发布次日排班
  - 标记请假
  - 查看受影响预约
  - 查看高质量修复方案判定结果
  - 查看保护性改签建议
  - 查看保护修复槽

## 3. 已完成但按文档保持默认关闭

以下项当前已按文档以“默认关闭”方式落地：

- `系统优化性改签` 按产品口径默认关闭。
- 当前未开放自动提交能力，这一点符合文档。

说明：

- 已新增 `GET /user/appointments/:id/reschedule-recommendations`
- 已新增用户端推荐卡片
- 当前保持默认关闭，开启后仅展示推荐，不自动改签

## 4. 本轮补充修正

本轮除功能推进外，还补齐并修正了以下验收尾项：

- 用户端已补“商户取消申请 -> 用户同意/拒绝”完整闭环。
- 用户端与商户端取消动作已升级为“先改签分流，再确认取消”。
- 改签接口已从“仅前端文案”升级为“后端实际过滤 + 前端实际展示”的阶段化实现。
- 已补 `GET /user/appointments/:id/reschedule-recommendations` 与用户端推荐卡片。
- 已补商户端“排班发布与异常修复”入口，用于发布次日排班、标记请假、查看受影响预约与保护修复槽。
- 已补 `GET /merchant/schedules/publishings` 与排班发布列表。
- `handlers/appointment_permission_test.go` 已补齐取消接口请求体，恢复 `go test ./...` 全绿。

## 5. 最终判断

如果按“核心业务是否可上线”判断：

- 可以判定为 `已完成主版本开发`

如果按“是否逐项满足两份文档的全部验收条目”判断：

- 当前可以判定为 `已完成并达到文档验收口径`

当前最准确的结论是：

- `预约新规重构开发已完成`
- `当前版本已实现文档与执行清单中的功能开发，并通过回归验证`
