# 未预约用户查看门店实时服务状态与排队进度功能设计方案

日期：2026-03-25

## 1. 修订结论

根据本次补充需求，这个能力的正确落点不是“新增一个实时排队系统”，而是：

- 不新增新的排队功能
- 不新增额外队列
- 不新增新的 `QueueType`
- 不新增新的排队表或异步排队链路

而是在现有项目代码基础上，直接利用当前两种既有模式的服务进度事实做推算：

1. 客服模式  
   根据 `核销 -> 选房 -> 选客服 -> 起单 -> 结单 -> 客服签到/忙闲状态` 推算门店当前服务负载。

2. 叫号模式  
   根据 `核销 -> 排队/叫号 -> 上号 -> 结号 -> 暂停叫号 -> 客服叫号状态` 推算门店当前服务负载。

最终提供的是一个“只读展示能力”，让未预约用户在门店公共页看到：

- 当前门店忙不忙
- 现在到店大概是否需要等
- 大概等多久
- 什么时候来更合适

不要求绝对准确，但要求基于现有服务流转，做到基本准确、稳定、可解释。

## 2. 需求边界

### 2.1 本次明确不做的事情

以下内容不在本方案范围内：

- 不新建任何新的排队业务对象
- 不新增“用户公开排队号”
- 不新增“预约排队”
- 不新增 Redis 队列类型
- 不新增快照表、读模型表作为一期前提
- 不改造现有预约/核销/叫号主流程为另一套流程

### 2.2 本次允许做的事情

为了把现有状态展示给未预约用户，允许新增：

- 一个新的只读公共接口
- 门店公共页上的一个新的展示模块
- 基于现有表和现有状态的聚合计算逻辑

这里新增的是“读接口和展示”，不是“新排队能力”。

## 3. 现有代码事实

### 3.1 当前公共门店页入口

未预约用户当前进入门店页的入口是：

- `backend/routes/routes.go`
- `backend/handlers/shop.go`
- `frontend/src/views/user/Shop.vue`

现有：

- `GET /user/s/:slug`
- `GET /user/s/id/:id`

只返回：

- 商户基础信息
- 收款配置
- 在售卡片

当前并没有把门店实时服务状态展示给未预约用户。

### 3.2 当前项目已经有统一的服务事实载体

现有项目里，最关键的实时事实不是某个“公开排队表”，而是：

- `service_sessions`

见：

- `backend/models/service_session.go`
- `backend/handlers/service_session_factory.go`
- `backend/handlers/service_session.go`
- `backend/sessionflow/finish.go`
- `backend/sessionflow/fail.go`

它已经统一承载：

- 现场单
- 预约到店单
- 客服模式会话
- 叫号模式会话

关键字段包括：

- `session_mode`
- `source_type`
- `status`
- `room_id`
- `technician_id`
- `predicted_ready_at`
- `scheduled_start_at`
- `scheduled_finish_at`
- `duration_minutes`

### 3.3 当前模式识别已经存在

见：

- `backend/models/session_mode.go`
- `backend/models/status_compat.go`

当前代码已经明确区分：

- `cs`：客服模式
- `qs`：自动叫号单窗口
- `qm`：自动叫号多窗口
- `qms`：人工叫号单窗口
- `qmm`：人工叫号多窗口

也已经支持通过 `NormalizeSessionStatus(...)` 统一读取裸状态。

这意味着：本方案完全可以基于“现有模式 + 现有状态机”推算，而不需要创造新的状态体系。

## 4. 两种模式的现有服务推进链路

## 4.1 客服模式现有链路

从当前代码看，客服模式核心链路是：

1. 核销后创建 `service_session`
2. 若商户启用房间，则进入 `room_selecting`
3. 选房后进入 `staff_selecting`
4. 选客服后进入 `start_pending`
5. 起单扫码后进入 `serving`
6. 结单后进入 `finished`

对应代码主要在：

- `backend/handlers/service_session_factory.go`
- `backend/handlers/service_session_user.go`
- `backend/handlers/service_session.go`
- `backend/sessionflow/finish.go`

客服模式下，以下操作本身就是“门店服务进度”：

- 核销
- 选房
- 选客服
- 起单
- 结单
- 客服签到与忙闲变化

也就是说，客服模式不需要额外排队对象，本身就已经有服务进度可读。

## 4.2 叫号模式现有链路

从当前代码看，叫号模式核心链路是：

1. 核销后创建 `service_session`
2. 现场单进入现有 `queue.Default` 的 `QueueTypeOnsite`
3. 会话初始状态为 `staff_selecting`
4. 根据模式不同推进：
   - 单窗口人工叫号：`staff_selecting -> delay_pending -> serving -> finished`
   - 多窗口叫号：`staff_selecting -> start_pending -> serving -> finished`
5. 结号/结单后通过 `sessionflow.FinishServiceSession(...)` 收尾
6. 暂停叫号由：
   - 商户级 `merchant.queue_paused`
   - 技师级 `technicians.queue_paused`
   控制

对应代码主要在：

- `backend/handlers/queue_status.go`
- `backend/handlers/service_session.go`
- `backend/handlers/technician_attendance.go`
- `backend/queue/queue.go`
- `backend/queue/redis_store.go`

因此，叫号模式下也不需要新增队列，只要读取现有：

- 核销后的 `service_sessions`
- 现有 `queue.Default`
- 叫号暂停状态
- 技师签到/忙闲状态

就能推算门店当前的服务压力。

## 4.3 现有技师状态已经足够表达服务供给

见：

- `backend/models/technician_attendance.go`
- `backend/models/models.go`
- `backend/handlers/technician_attendance.go`

当前项目已有：

- `technician_attendances.status = idle / busy / paused / rest`
- `technicians.queue_paused`
- `merchant.queue_paused`

这三类状态已经足够推断：

- 当前可立即承接服务的人数
- 当前服务中的人数
- 当前暂停接待的人数
- 叫号模式下新客能否继续被推进

## 5. 当前问题不在于“没有队列”，而在于“没有对外展示口径”

当前门店公共页缺的不是底层事实，而是一个面向未预约用户的只读解释层。

当前缺口有两个：

1. 没有公共接口把现有状态聚合成“门店实时服务状态”
2. 没有把这些状态翻译成用户能看懂的文案和等待预测

因此，本次方案重点应从“新排队设计”改成“现有服务状态的读时推算与展示”。

## 6. 设计目标重构

### 6.1 对用户的目标

未预约用户打开门店页时，希望直接看到：

- 当前空闲还是忙碌
- 现在去大概要不要等
- 当前是卡在选客服、待起单、服务中，还是叫号暂停
- 建议什么时候去更容易快速开始服务

### 6.2 对实现的目标

实现上要做到：

- 完全复用现有状态机
- 同时兼容客服模式与叫号模式
- 结果可解释
- 精度不要求绝对准确，但不能明显失真
- 对现有业务链路零侵入或低侵入

## 7. 统一的实时服务状态读模型

建议不要再把这个能力继续命名为“live queue”。

更准确的语义应是：

- `门店实时服务状态`
- 或 `门店实时服务负载`

因此建议公共接口命名为：

- `GET /user/merchants/:id/live-service-status`

如果后续为了兼容旧文档必须保留 `live-queue` 叫法，也建议只作为兼容别名，内部语义仍然按“服务状态推算”处理。

### 7.1 通用字段

建议统一返回：

- `enabled`
- `service_mode`
- `business_state`
- `load_level`
- `summary_text`
- `present_count`
- `waiting_count`
- `serving_count`
- `idle_staff_count`
- `busy_staff_count`
- `paused_staff_count`
- `estimated_wait_minutes`
- `recommended_arrival_at`
- `recommend_appointment`
- `advice_text`
- `snapshot_at`
- `refresh_after_seconds`

### 7.2 阶段计数字段

为了让“服务进度”可解释，建议额外返回阶段计数：

- `room_selecting_count`
- `staff_selecting_count`
- `start_pending_count`
- `delay_pending_count`
- `appointment_waiting_count`
- `queue_waiting_count`
- `queue_paused`

说明：

- 在客服模式下，重点用 `room_selecting / staff_selecting / start_pending / serving`
- 在叫号模式下，重点用 `queue_waiting / start_pending 或 delay_pending / serving / queue_paused`

## 8. 状态口径与公开展示含义

建议统一基于 `NormalizeSessionStatus(...)` 处理基础状态。

| 基础状态 | 客服模式含义 | 叫号模式含义 | 计入到店 | 计入待服务 | 计入服务中 | 对等待预测影响 |
|---|---|---|---|---|---|---|
| `room_selecting` | 已核销待选房 | 历史兼容态 | 是 | 是 | 否 | 弱 |
| `room_locked` | 已选房待选客服 | 迁移兼容态 | 是 | 是 | 否 | 中 |
| `staff_selecting` | 已核销待选客服 | 待叫号/待分配 | 是 | 是 | 否 | 中到强 |
| `appointment_waiting` | 预约到店优先等待 | 历史兼容态 | 是 | 是 | 否 | 强 |
| `start_pending` | 已分配客服待起单 | 已分配客服待上号 | 是 | 是 | 否 | 强 |
| `delay_pending` | 待开始服务兼容态 | 已叫号待上号 | 是 | 是 | 否 | 强 |
| `serving` | 服务中 | 服务中 | 是 | 否 | 是 | 强占用 |
| `auto_finishing` | 待自动结单 | 待自动结号 | 是 | 否 | 是 | 强占用 |
| `timeout_waiting` | 不推荐公开展示 | 过号等待 | 否 | 否 | 否 | 默认不计入主预测 |
| `finished` | 已完成 | 已完成 | 否 | 否 | 否 | 不计入 |
| `canceled` | 已取消 | 已取消 | 否 | 否 | 否 | 不计入 |
| `timeout_failed` | 失败 | 过号失效 | 否 | 否 | 否 | 不计入 |

## 9. 推算原则

### 9.1 客服模式：基于服务推进状态推算

客服模式不使用任何额外排队对象，直接基于现有服务进度推算。

核心输入：

- `service_sessions`
- `technician_attendances`
- `rooms`
- `merchant_projects.duration`

推算逻辑：

1. `serving / auto_finishing` 表示当前产能已被占用
2. `start_pending` 表示已经接近开始服务，对新客等待影响最大
3. `staff_selecting / room_locked` 表示仍在完成服务前置步骤，对等待有中等影响
4. `room_selecting` 表示已到店但还未完成前置动作，对等待有弱影响
5. `appointment_waiting` 表示预约已到店，应优先消化

因此，客服模式预测不需要真实排队顺序，而是按“现有服务进度阶段”估算阻塞程度。

### 9.2 叫号模式：基于现有叫号推进状态推算

叫号模式只读取现有 `queue.Default`，不新增任何队列。

核心输入：

- `service_sessions`
- `queue.Default` 现有 `QueueTypeOnsite`
- `technician_attendances`
- `merchant.queue_paused`
- `technicians.queue_paused`

推算逻辑：

1. `staff_selecting` + 现有队列票据 = 当前仍在排队中的现场客
2. `delay_pending` = 已叫号待上号，已经非常接近占用服务能力
3. `start_pending` = 多窗口模式下已分配客服待上号
4. `serving / auto_finishing` = 当前实际服务中
5. `queue_paused` = 新客虽然能核销，但推进速度会显著下降，应显著上调等待建议

### 9.3 预约到店事实要参与预测，但不单独形成新队列

预约到店后，当前代码会创建：

- `usage`
- `service_session(source_type=appointment)`

并进入：

- `appointment_waiting`
- 或 `start_pending`

因此它们已经是现有服务状态的一部分。

本方案只把这些状态视为“应优先处理的现有工作量”，不新建任何“预约排队队列”。

## 10. 预计等待时间的推算方案

本能力只要求“基本准确”，不追求排班系统级别的精确预测，因此建议采用“服务槽位 + 现有工作量”推算法。

### 10.1 第一步：计算当前有效服务槽位

服务槽位来源于当前门店真实供给。

规则：

1. 只统计当日已签到、未签退的技师
2. `status = idle / busy` 视为可服务体系内的人
3. `status = paused / rest` 不计入当前可承接产能
4. 叫号模式下若 `technicians.queue_paused = true`，该技师不计入新客承接产能
5. 若商户开启房间：
   - 最终并发上限取 `min(有效技师数, 可用房间数)`
6. 单窗口人工/自动叫号：
   - 即便存在多个技师，也按当前实际模式把可承接窗口数收敛到 1

### 10.2 第二步：计算每个服务槽位的释放时间

对每个当前占用中的技师，计算 `available_at`：

优先级：

1. `predicted_ready_at`
2. `scheduled_finish_at`
3. `started_at + duration_minutes`
4. 兜底 `now + baseline_service_minutes`

这样可以避免仅凭人数估算导致偏差过大。

### 10.3 第三步：把当前未完成的工作量转成“阻塞项”

对于尚未进入 `serving` 但已经形成到店事实的会话，不直接当作“完整一单”，而是按当前阶段给予不同权重。

建议权重：

- `appointment_waiting / start_pending / delay_pending`：`1.0`
- `staff_selecting / room_locked`：`0.8`
- `room_selecting`：`0.5`

说明：

- 越接近真正开始服务，权重越高
- 越早期的前置步骤，对新客实际等待影响越弱
- 这比“所有等待中的人都算完整一单”更接近真实情况

### 10.4 第四步：模式化排序

#### 客服模式

按以下顺序视为优先被消化：

1. `appointment_waiting`
2. `start_pending`
3. `staff_selecting / room_locked`
4. `room_selecting`

同一阶段内按：

- `created_at asc`

兜底。

#### 叫号模式

按以下顺序：

1. `delay_pending / start_pending`
2. `staff_selecting`

其中 `staff_selecting` 若存在现有 `queue.Default` 号序，则按现有队列号排序；取不到号时按 `created_at asc` 兜底。

### 10.5 第五步：把“新来的未预约用户”插入末尾

不创建任何真实队列记录，只在内存计算中增加一个“虚拟新用户”。

然后根据：

- 当前服务槽位释放时间
- 当前待消化工作量
- 当前阶段权重

推导他可能轮到开始服务的时间，得到：

- `estimated_wait_minutes`

### 10.6 第六步：门店基准服务时长

为了做基本准确的预测，建议采用门店的“基准项目时长”作为虚拟新客的服务占用时长。

建议口径：

1. 优先取当前启用项目 `merchant_projects.duration` 的中位数
2. 若项目为空：
   - 叫号模式兜底 `15`
   - 客服模式兜底 `50`

这个兜底直接复用当前 `createServiceSessionForUsage(...)` 的默认值，和现有代码口径一致。

### 10.7 第七步：建议到店时间

设一个可接受等待阈值：

- 一期默认 `10` 分钟

规则：

- 若 `estimated_wait_minutes <= 10`，建议立即到店
- 若 `estimated_wait_minutes > 10`，建议时间为 `now + (estimated_wait_minutes - 10) 分钟`

这样给用户的是“什么时候来更容易在 10 分钟内开始服务”，比纯展示等待时间更有决策价值。

## 11. 忙闲等级与文案

建议统一规则：

- `idle`：`0 ~ 5` 分钟
- `light`：`6 ~ 15` 分钟
- `busy`：`16 ~ 30` 分钟
- `crowded`：`> 30` 分钟
- `unavailable`：打烊、无有效服务槽位、或叫号暂停且短时无法推进

### 11.1 客服模式文案示例

- `当前稍忙，2 人待选客服，1 人待起单，3 人服务中`
- `现在到店预计等待 12 分钟`
- `建议 14:30 后到店，体验更稳定`

### 11.2 叫号模式文案示例

- `当前较忙，3 人排队，1 人待上号，2 人服务中`
- `预计等待 18 分钟`
- `当前叫号已暂停，建议稍后到店或先联系门店`

## 12. 接口设计

### 12.1 推荐接口

建议新增只读公共接口：

- `GET /user/merchants/:id/live-service-status`

用途：

- 只做展示
- 不创建任何会话
- 不写任何队列
- 不改变现有业务状态

### 12.2 返回示例

```json
{
  "data": {
    "enabled": true,
    "service_mode": "customer_service",
    "business_state": "open",
    "load_level": "light",
    "summary_text": "2人待选客服，1人待起单，3人服务中",
    "present_count": 6,
    "waiting_count": 3,
    "serving_count": 3,
    "idle_staff_count": 1,
    "busy_staff_count": 3,
    "paused_staff_count": 0,
    "room_selecting_count": 0,
    "staff_selecting_count": 2,
    "start_pending_count": 1,
    "delay_pending_count": 0,
    "appointment_waiting_count": 0,
    "queue_waiting_count": 0,
    "queue_paused": false,
    "estimated_wait_minutes": 12,
    "recommended_arrival_at": "2026-03-25T14:30:00+08:00",
    "recommend_appointment": false,
    "advice_text": "现在到店可接受，若希望更快开始服务，建议14:30后到店",
    "snapshot_at": "2026-03-25T14:18:00+08:00",
    "refresh_after_seconds": 20
  }
}
```

### 12.3 叫号模式返回示例

```json
{
  "data": {
    "enabled": true,
    "service_mode": "queue_manual_single",
    "business_state": "open",
    "load_level": "busy",
    "summary_text": "3人排队，1人待上号，2人服务中",
    "present_count": 6,
    "waiting_count": 4,
    "serving_count": 2,
    "idle_staff_count": 0,
    "busy_staff_count": 2,
    "paused_staff_count": 1,
    "room_selecting_count": 0,
    "staff_selecting_count": 3,
    "start_pending_count": 0,
    "delay_pending_count": 1,
    "appointment_waiting_count": 0,
    "queue_waiting_count": 3,
    "queue_paused": false,
    "estimated_wait_minutes": 18,
    "recommended_arrival_at": "2026-03-25T14:42:00+08:00",
    "recommend_appointment": true,
    "advice_text": "当前较忙，建议先预约或 14:42 后到店",
    "snapshot_at": "2026-03-25T14:18:00+08:00",
    "refresh_after_seconds": 20
  }
}
```

## 13. 后端落地建议

### 13.1 路由

修改：

- `backend/routes/routes.go`

新增：

```go
user.GET("/merchants/:id/live-service-status", handlers.GetMerchantLiveServiceStatus)
```

### 13.2 建议新增文件

- `backend/handlers/live_service_status.go`

建议拆分函数：

- `GetMerchantLiveServiceStatus(c *gin.Context)`
- `buildMerchantLiveServiceStatus(tx *gorm.DB, merchant models.Merchant, now time.Time)`
- `loadActiveServiceSessions(...)`
- `loadEffectiveStaffSlots(...)`
- `buildModeStageSummary(...)`
- `estimateWalkInWaitByCurrentProgress(...)`
- `buildAdviceText(...)`

### 13.3 读取边界

一期只读现有数据源：

- `merchants`
- `service_sessions`
- `technician_attendances`
- `technicians`
- `rooms`
- `merchant_projects`
- `queue.Default`

明确不新增：

- 新队列表
- 新快照表
- 新排队任务
- 新调度器

## 14. 前端落地建议

### 14.1 API 层

修改：

- `frontend/src/api/index.js`

建议增加：

```js
getLiveServiceStatus: (merchantId) => api.get(`/user/merchants/${merchantId}/live-service-status`)
```

### 14.2 页面接入

修改：

- `frontend/src/views/user/Shop.vue`

新增模块建议名称：

- `门店实时服务状态`

不建议继续只写“实时排队”，因为客服模式下用户看到的不只是排队，还包括：

- 选房
- 选客服
- 待起单
- 服务中

### 14.3 展示结构建议

1. 第一行

- 忙闲标签
- 更新时间

2. 第二行

- 大字摘要：`2人待选客服，1人待起单，3人服务中`

3. 第三行

- 当前预计等待
- 建议到店时间

4. 第四行

- 建议文案

## 15. 一期实施范围

### 15.1 一期做什么

- 新增只读公共接口
- 公共页展示门店实时服务状态
- 基于现有客服模式和叫号模式状态做推算
- 输出基本准确的等待预测

### 15.2 一期不做什么

- 不新增队列
- 不新增预约队列
- 不新增新的排队号
- 不改变现有叫号推进逻辑
- 不改造预约履约链路

## 16. 测试重点

建议至少覆盖以下场景。

### 16.1 客服模式

1. 核销后无人选房，显示 `room_selecting`
2. 已选房待选客服，显示 `staff_selecting`
3. 已选客服待起单，显示 `start_pending`
4. 多个客服 `idle / busy / paused` 混合
5. 房间数少于技师数时的并发收敛

### 16.2 叫号模式

1. `staff_selecting` 且队列中仍有票据
2. `delay_pending` 已叫号待上号
3. 多窗口 `start_pending`
4. 商户 `queue_paused = true`
5. 技师 `queue_paused = true`
6. `serving -> finished -> MarkDone`

### 16.3 预约到店插入

1. 预约到店进入 `appointment_waiting`
2. 预约到店进入 `start_pending`
3. 预约等待对新未预约用户等待预测的影响

## 17. 最终建议

这份方案修订后的核心方向是：

- 不再把问题理解为“给未预约用户再造一个排队系统”
- 而是把现有客服模式、叫号模式里的真实服务进度，整理成一个统一的公开展示视图

一期最佳落地路径是：

1. 基于现有 `service_sessions` 读取当前服务进度
2. 基于现有 `technician_attendances` 和房间配置计算真实供给
3. 仅在叫号模式下读取现有 `queue.Default` 作为排序参考
4. 输出 `门店实时服务状态`
5. 在用户门店页展示“状态 + 进度 + 基本等待预测”

这样既满足你“不要新增排队功能及额外队列”的约束，又能利用当前项目已有代码快速落地，并保持方案可解释、可执行。
