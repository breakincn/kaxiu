# 改签及补偿计划方案与实施对照清单

## 目标
在既有“预约确认 -> 占产能 -> 到店核销 -> ServiceSession”闭环基础上，补齐商户端正式的改签与补偿能力，避免继续依赖单纯备注处理预约冲突。

本次实现聚焦三个核心能力：
- 改签到新时间
- 补偿类型选择并执行
- 原预约关闭 + 新预约生成并形成可追溯关系

## 功能方案

### 一、动作边界

#### 取消
- 含义：本次预约终止，不再继续服务。
- 适用状态：`pending`、`confirmed`。
- 当前实现：保留原有取消能力。

#### 改签
- 含义：本次预约继续，但改到新的时间，必要时改客服或项目。
- 适用状态：`confirmed`、`arrived`、`failed`。
- 实施规则：
  - 原预约不直接改时间，必须“关闭旧预约 + 创建新预约”。
  - 新预约仍走预约保护规则，校验营业时间和客服可用性。
  - 若旧预约已经到店但还未开始正式服务，先撤销本次到店占用，再完成改签。

#### 补偿
- 含义：因门店未兑现预约承诺，对客户做正式补回。
- 展示原则：
  - `confirmed / 待到店` 不提前显示补偿。
  - `arrived` 且已产生等待/服务冲突时显示补偿。
  - `completed`、`failed` 允许补录补偿。
- 当前补偿类型：
  - `extra_times`：补次数，立即增加卡片总次数与剩余次数
  - `extend_minutes`：补时长，立即延长当前关联服务会话
  - `discount_note`：优惠减免记录，形成正式补偿单与备注
  - `other_note`：其他补偿记录，形成正式补偿单与备注

### 二、改签流程
1. 商户在预约卡片点击“改签”。
2. 弹窗选择新的日期、时间段、客服，并填写改签原因。
3. 系统读取新的可用时段与客服候选，并复用预约保护策略判断 `safe / conditional / unavailable`。
4. 提交后在事务内执行：
   - 锁定旧预约
   - 校验旧预约状态是否允许改签
   - 若已到店但未开始服务，撤销 usage / service_session 占用
   - 创建新预约
   - 关闭旧预约并写入 `closed_reason=rescheduled`
   - 互相回写 `replaces_appointment_id / replaced_by_appointment_id`
5. 返回新旧预约，前端刷新列表。

### 三、补偿执行流程
1. 商户在预约卡片点击“补偿”。
2. 弹窗选择补偿类型、数值、原因和备注。
3. 系统创建正式补偿记录 `appointment_compensations`。
4. 按补偿类型立即执行：
   - `extra_times`：回充卡次数
   - `extend_minutes`：延长关联服务会话时长和预计结束时间
   - `discount_note / other_note`：保留正式记录与预约处理备注
5. 补偿记录状态推进为 `applied`，并回写预约的 `resolution_note`。

## 已完成

### 后端
- 新增预约改签关系字段：
  - `closed_reason`
  - `closed_by_type`
  - `closed_by_id`
  - `reschedule_reason`
  - `replaced_by_appointment_id`
  - `replaces_appointment_id`
- 新增预约补偿模型与表：`appointment_compensations`
- 新增迁移：
  - `2026031702 add_appointment_reschedule_and_compensation`
- 新增接口：
  - `POST /merchant/appointments/:id/reschedule`
  - `GET /merchant/appointments/:id/compensations`
  - `POST /merchant/appointments/:id/compensations`
- 实现改签事务逻辑：
  - 原预约关闭
  - 新预约生成
  - 新旧预约关联
  - 已到店未开始服务时撤销核销占用
- 实现补偿执行逻辑：
  - `extra_times`
  - `extend_minutes`
  - `discount_note`
  - `other_note`
- 新增关键注释，覆盖：
  - sqlite 测试环境下预约时间加载兼容
  - 到店后改签前的占用回滚
  - 不同补偿类型对资源的直接影响

### 前端
- 商户预约卡片动作改为按状态显示：
  - `confirmed`：`取消`、`改签`
  - `arrived`：`继续等待原客服`、`改派其他客服`、满足条件时 `补偿`
- 新增“改签到新时间”弹窗：
  - 日期选择
  - 时间段选择
  - 客服选择
  - 改签原因
- 新增“预约补偿”弹窗：
  - 补偿类型
  - 数值
  - 原因
  - 备注
- 在预约卡片中展示补偿记录与结果

### 测试与验证
- 新增后端测试：
  - 改签后旧预约关闭且新预约生成
  - 补次数会正确回充卡次数
- 已通过验证：
  - `go test ./handlers`
  - `go test ./...`
  - `npm run build:merchant`
  - `npm run build:user`

## 保留兼容
- 历史 `resolution_note` 仍保留，继续作为预约处理摘要展示。
- `discount_note` 与 `other_note` 当前落为正式补偿记录 + 备注，不直接联动营销或财务系统。
- 原取消接口与既有预约列表接口保持兼容。

## 下一步待完成
- 新增改签预览接口，把“改签前预校验”从前端组合请求收敛为单接口。
- 给补偿增加更细的执行结果展示，例如“补了哪张卡、补了多少次、补到了哪笔服务”。
- 若后续接入优惠券/营销系统，可把 `discount_note` 升级为真正的券或减免权益发放。
- 若后续接入完整财务补偿体系，可为补偿记录增加审批、撤销和审计流。
