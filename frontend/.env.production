# =============================================================================
# 卡包系统 - 前端生产环境配置文件
# =============================================================================
# 使用说明：
# 1. 此文件用于生产环境构建时的配置
# 2. 构建命令：npm run build:user、npm run build:merchant、npm run build:admin
# 3. 构建后的配置会写入到 dist-user/、dist-merchant/、dist-admin/ 目录

# =============================================================================
# 生产环境配置
# =============================================================================
NODE_ENV=production
VITE_DISABLE_PWA=false

# =============================================================================
# 用户端生产配置
# =============================================================================
VITE_APP_TARGET=user
VITE_API_BASE_URL=https://api.kabao.app/api

# =============================================================================
# 商户端生产配置（构建时通过脚本覆盖）
# =============================================================================
# 商户端构建时，package.json 中的 build:merchant 脚本会设置：
# VITE_APP_TARGET=merchant
# VITE_API_BASE_URL=https://api.kabao.app/api

# =============================================================================
# 平台端生产配置（构建时通过脚本覆盖）
# =============================================================================
# 平台端构建时，package.json 中的 build:admin 脚本会设置：
# VITE_APP_TARGET=admin
# VITE_API_BASE_URL=https://api.kabao.app/api
