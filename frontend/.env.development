# =============================================================================
# 卡包系统 - 前端开发环境配置文件
# =============================================================================
# 使用说明：
# 1. 此文件用于本地开发环境的前端配置
# 2. 根据要启动的服务类型，取消注释对应的配置块
# 3. 启动命令：npm run dev:user、npm run dev:merchant、npm run dev:admin

# =============================================================================
# 通用开发配置
# =============================================================================
NODE_ENV=development
VITE_DISABLE_PWA=true

# =============================================================================
# 用户端配置 - 取消注释以下内容启动用户端
# =============================================================================
# VITE_APP_TARGET=user
# VITE_API_BASE_URL=http://localhost:8081/api

# =============================================================================
# 商户端配置 - 取消注释以下内容启动商户端  
# =============================================================================
# VITE_APP_TARGET=merchant
# VITE_API_BASE_URL=http://localhost:8082/api

# =============================================================================
# 平台端配置 - 取消注释以下内容启动平台端
# =============================================================================
# VITE_APP_TARGET=admin
# VITE_API_BASE_URL=http://localhost:8083/api

# =============================================================================
# 生产环境模拟配置（可选）
# =============================================================================
# 如果需要模拟生产环境，可以使用以下配置：
# VITE_APP_TARGET=user
# VITE_API_BASE_URL=https://api.kabao.app/api
# 或者：
# VITE_APP_TARGET=merchant  
# VITE_API_BASE_URL=https://api.kabao.app/api
# 或者：
# VITE_APP_TARGET=admin
# VITE_API_BASE_URL=https://api.kabao.app/api
